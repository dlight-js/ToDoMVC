import { ViewParticle } from '@dlightjs/reactivity-parser';
import Babel, { types } from '@babel/core';

type SubViewPropMap = Record<string, string[]>;
interface ViewGeneratorConfig {
    babelApi: typeof Babel;
    className: string;
    importMap: Record<string, string>;
    subViewPropMap: SubViewPropMap;
    templateIdx: number;
}

declare function generateView(viewParticles: ViewParticle[], config: ViewGeneratorConfig): [types.BlockStatement, types.ClassProperty[], number];
declare function generateSubView(viewParticlesWithPropertyDep: ViewParticle[], viewParticlesWithIdentityDep: ViewParticle[], propNode: types.ObjectPattern, config: ViewGeneratorConfig): [types.BlockStatement, types.ClassProperty[], number];

export { SubViewPropMap, ViewGeneratorConfig, generateSubView, generateView };
